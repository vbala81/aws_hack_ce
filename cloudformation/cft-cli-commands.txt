S3 bucket + lambda invoke permission + lambda function + event trigger
aws cloudformation create-stack --stack-name s3-lambda-stack --template-body file://s3-lambda-config.yaml --parameters file://s3-lambda-config-params.json --capabilities CAPABILITY_NAMED_IAM   
aws cloudformation update-stack --stack-name s3-lambda-stack --template-body file://s3-lambda-config.yaml --parameters file://s3-lambda-config-params.json --capabilities CAPABILITY_NAMED_IAM